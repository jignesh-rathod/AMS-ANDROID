<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.AMS" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="colorPrimary">@color/primary_light</item>
        <item name="colorSecondary">@color/secondary_light</item>
        <item name="android:windowBackground">@color/background_light</item>

        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/background_light</item>

        <item name="android:windowLightNavigationBar" tools:ignore="NewApi">true</item>
        <item name="android:navigationBarColor">@color/background_light</item>

    </style>

    <style name="Theme.AMS" parent="Base.Theme.AMS" />

    <style name="Theme.AMS.SplashScreen" parent="Theme.SplashScreen">

        <item name="android:windowSplashScreenBackground" tools:ignore="NewApi">@color/background_light</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_screen_logo</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.AMS</item>

    </style>

    <!-- Button styles -->
    <style name="DefaultButton">
        <item name="cornerSize">5dp</item>
    </style>

    <!-- BottomNav styles -->
    <style name="BottomNavTextStyle" parent="TextAppearance.AppCompat">
        <item name="android:fontFamily">@font/poppins_semibold</item>
    </style>

</resources>